<div>
  <div class="mail_feedback">
    <span>
      <a href="mailto:someone@oxygenxml.com">Give us feedback</a>
    </span>
  </div>
  
  <script type="text/javascript">
    <!-- 
      const publication = Array.from(document.querySelectorAll('.wh_publication_title a'))
        .map(a => a.textContent.trim())
        .join(' ');
      
      const breadcrumb = Array.from(document.querySelectorAll('.wh_breadcrumb a'))
        .map(a => a.textContent.trim())
        .join(' / ');
      
      const subjectText = breadcrumb
        ? `${publication} - ${breadcrumb}`
        : publication;
      
      const subject = encodeURIComponent(subjectText);
      
      document.querySelectorAll('a[href^="mailto:"]').forEach(link => {
        link.href = `mailto:someone@example.com?subject=${subject}`;
      });
    -->
  </script>
  
  <div>
    <span>Generated by <a class="oxyFooter" href="http://www.oxygenxml.com/xml_webhelp.html" target="_blank"> &lt;oXygen/&gt; XML WebHelp </a></span>
  </div>
</div>
